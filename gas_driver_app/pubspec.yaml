name: gas_driver_app
description: "A new Flutter project for GasFlow Driver."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # 🌐 Networking
  http: ^1.1.0
  
  # 🔑 Session & Local Storage
  shared_preferences: ^2.2.2
  
  # 📦 Offline Database (Hive)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  
  # 🛰️ Location & Maps
  geolocator: ^10.1.0
  google_maps_flutter: ^2.5.0
  
  # 📞 System Integrations
  url_launcher: ^6.2.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # 🏗️ Hive Code Generation
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  
  # 🚀 Icon Generation
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^3.0.0

# 🛰️ APP ICON CONFIGURATION
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#121212" 
  adaptive_icon_foreground: "assets/icon/app_icon.png"

flutter:
  uses-material-design: true

  # 📂 REGISTER ASSETS
  assets:
    - assets/icon/app_icon.png